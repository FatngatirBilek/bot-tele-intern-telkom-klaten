services:
  ticket-bot:
    build: .
    container_name: tele-ticket-bot
    restart: always
    env_file:
      - .env
    volumes:
      - .:/app # Biar kalau lo ganti script, di container juga berubah
